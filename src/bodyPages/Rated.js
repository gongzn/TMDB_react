import React from "react";
import { Typography, Grid, Box } from "@mui/material";

export default function Rated() {
  return (
    <Box sx={{ display: "flex" }} justifyContent="center" margin={2}>
      <Typography variant="h4">Rated Movies</Typography>
    </Box>
  );
}
