import * as React from 'react';
import { createTheme} from '@mui/material/styles';


export const theme = createTheme({
    palette: {
      primary: {main: "#0d253f", dark: "#000000"},
      secondary: {main: "#01b4e4"},
      success: {main: "#90cea1"}
    }
  })

